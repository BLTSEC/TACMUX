#!/usr/bin/env bash
set -euo pipefail

case "${1:-}" in
    attach|attach-session|switch-client) exit 0 ;;
esac

exec "${TACMUX_REAL_TMUX:?TACMUX_REAL_TMUX is required}" -S "$TACMUX_TEST_SOCKET" "$@"
