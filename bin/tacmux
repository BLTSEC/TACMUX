#!/bin/sh

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
ROOT=$(dirname "$SCRIPT_DIR")

if [ -x "$ROOT/.venv/bin/tacmux" ]; then
    exec "$ROOT/.venv/bin/tacmux" "$@"
fi

PYTHONPATH="$ROOT/src${PYTHONPATH:+:$PYTHONPATH}"
export PYTHONPATH
exec python3 -m tacmux.cli "$@"
